version: '2'

services:
  postgres:
    container_name: pgdb
    image: postgres
    environment:
            POSTGRES_PASSWORD: "machine"
            POSTGRES_USER: "machine"
  web:
    build: .
    container_name: webapp
    environment:
      DATABASE_HOST: "postgres"
      RAILS_ENV: "development"
    entrypoint:
    - tail
    - -f
    - /var/log/dmesg
